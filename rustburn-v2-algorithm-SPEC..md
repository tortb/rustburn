# rustburn v2 算法重构规格文档

> 本文档补充/替代原 SPEC.md 第 7 节（热力值公式引擎）。目标：把三维度公式
> 重构为五维度、Role 分层架构，同时修复三个已确认的设计缺陷：
> ① 分数只涨不跌 ② 全局统一阈值对特定领域仓库（如解析器）不公平
> ③ 缺少测试覆盖率/测试质量/重复代码维度。
>
> **本文档存在的原因**：过往几轮开发中，AI 反复出现"方向做对了、但关键
> 细节留了空子"的模式——编造不存在的 CVE 编号、trend_history 全是空
> 数组却假装功能存在、编译产物目录没排除干净。本文档因此把每一条要求
> 都配一个"如何验证"和"禁止的偷懒方式"，不允许用"基本完成"搪塞。

---

## 0. 强制阅读顺序

开发 AI 必须先完整阅读本文档全部章节，再动手写代码。**不允许边读边写、
读一节写一节**——因为后面章节的"禁止事项"经常是对前面章节实现方式的
约束，跳着做容易先把坑挖好。

---

## 1. 架构总览：两层 Role 拆分

```
LanguageAdapter（语言适配层，每加一种语言只需实现这一个 trait）
DimensionAnalyzer（五个打分角色，互相独立，不感知语言细节）
  ├── ComplexityAnalyzer      依赖 LanguageAdapter
  ├── DuplicationAnalyzer     依赖 LanguageAdapter
  ├── TestAnalyzer            不依赖 LanguageAdapter（走文件命名约定+覆盖率报告）
  ├── ChangeRiskAnalyzer      完全语言无关（只吃 git log）
  └── DependencyAnalyzer      完全语言无关（只吃锁文件）
scoring.rs（只做归一合成，不含任何维度的具体计算逻辑）
```

### 1.1 Trait 定义（严格照此实现，不允许简化签名）

```rust
trait LanguageAdapter: Send + Sync {
    fn language(&self) -> Language;
    fn parse(&self, source: &str) -> Result<Tree, ParseError>;
    fn is_branch_node(&self, kind: &str) -> bool;
    fn is_function_node(&self, kind: &str) -> bool;
    fn is_chained_else_if(&self, node: &Node) -> bool;
}

trait DimensionAnalyzer: Send + Sync {
    fn name(&self) -> &'static str;
    fn analyze(&self, ctx: &FileContext) -> DimensionResult;
}

struct DimensionResult {
    raw_value: f64,
    risk_score: f64,        // 0-100，该维度独立分数，不是最终合成分
    confidence: Confidence,
    detail: serde_json::Value,
}

enum Confidence {
    Full,
    DataMissing(String),   // 必须带具体原因，不允许空字符串或泛泛的"missing"
    NotApplicable,         // 该语言/该场景不支持此维度
}
```

**【禁止事项 1.1-A】**：不允许把 `Confidence::DataMissing` 或 `NotApplicable`
的判断逻辑写成"默认返回 Full，只有显式检测到错误才降级"。正确顺序是反过来：
**默认假设数据不完整，只有在明确验证过数据来源真实可靠后，才允许标记为 Full**。
这一条是防止"没测试报告却假装测试分析完整跑完了"这类问题的核心防线。

**验收方式**：单元测试里构造一个"没有任何测试文件、没有覆盖率报告"的
mock 仓库，断言 `TestAnalyzer::analyze()` 返回的 `confidence` 必须是
`DataMissing(_)`，且 `risk_score` 不能是 0（0 意味着"零风险"，但真实
含义是"不知道"，这两者语义不同，不允许混淆）。

---

## 2. ComplexityAnalyzer（复杂度维度）

### 2.1 计算方式（与原 SPEC 第 5 节一致，此处不重复贴代码，仅列新增约束）

**【禁止事项 2-A】**：`is_chained_else_if` 的实现不允许用"检测到 else 分支
里只有一个 if 节点就直接判定为链式"这种过度简化的规则。必须同时满足：
① else 分支只包含单个语句 ② 该语句是 if 节点 ③ 这个 if 节点没有被赋值/
返回等表达式上下文包裹（否则是表达式风格的 if-else，应按原 SPEC 5.3 节
的表达式风格规则单独处理，不能和链式判断混为一谈）。

**验收方式**：至少 3 组测试用例——纯链式、纯表达式风格 if-else、真实
嵌套——分别验证嵌套深度统计结果符合预期，不能只测一种就算通过。

---

## 3. DuplicationAnalyzer（重复代码维度，新增）

### 3.1 算法

```
1. 对每个函数/代码块生成"结构哈希"：遍历该节点子树，把所有标识符
   （变量名、函数名等）替换成占位符 IDENT，保留语法结构和字面量类型
   （比如区分数字字面量和字符串字面量，但不保留具体值），再做哈希
2. 只对超过 6 行的代码块参与判重（避免"return null;"这种短语句
   被误判为"重复"）
3. 哈希相同的代码块归为一组，统计每个文件参与"重复组"的行数占比
duplication_risk_value = min(100, 该文件重复行数占比 × 150)
```

**【禁止事项 3-A】**：不允许用简单的文本级别（逐行字符串比较）判重
代替结构哈希。文本判重会被"改一个变量名"轻易绕过，这正是防刷分设计
要堵住的漏洞——如果重复检测能被简单的重命名绕过，这个维度就是摆设。

**【禁止事项 3-B】**：不允许把"结构哈希"简化成"只用节点类型序列的
哈希而完全丢弃字面量类型信息"。比如 `if (x > 0)` 和 `if (x == "")`
如果只按节点类型（都是 binary_expression 内嵌 if）会被误判成重复，
但语义完全不同。必须区分字面量的**类型**（数字/字符串/布尔），
只是不保留具体值。

**验收方式**：
- 测试 A：把同一段 10 行逻辑复制 3 份到不同文件，仅改变量名，断言
  三个文件的 `duplication_risk_value` 都显著高于仓库均值
- 测试 B：构造两段结构相似但字面量类型不同的代码（如上面的例子），
  断言不会被误判为重复
- 测试 C：构造多段 3-5 行的短代码块（哪怕完全相同），断言不计入
  重复统计（低于 6 行阈值）

---

## 4. TestAnalyzer（测试覆盖与质量维度，新增）

### 4.1 计算方式

```
test_risk_value = 覆盖率缺口 × 0.5 + 测试密度缺口 × 0.3 + 断言密度缺口 × 0.2

覆盖率缺口 = 100 - 覆盖率%   （仅当能解析到 lcov/cobertura 报告时使用）
测试密度缺口 = 100 - min(100, 对应测试文件总行数 / 该文件行数 × 100)
断言密度缺口 = 100 - min(100, 测试文件里 assert/expect 类调用数
                          / 测试文件行数 × 系数(建议20))
```

### 4.2 找"对应测试文件"的规则（必须写死，不允许 AI 自由发挥）

按以下优先级依次尝试，找到第一个命中即停止：
1. 同目录下 `<文件名>_test.<ext>` 或 `<文件名>.test.<ext>` 或
   `test_<文件名>.<ext>`（覆盖 Rust/JS/Python 等常见命名习惯）
2. Rust 专属：文件内部的 `#[cfg(test)] mod tests` 块（这种情况测试代码
   和实现代码在同一个文件里，密度统计要单独处理：从文件总行数里减去
   test mod 的行数作为"实现行数"分母，test mod 内部行数作为"测试行数"）
3. 仓库里存在独立的 `tests/` 目录，且文件名能通过路径映射规则关联
   （比如 `src/foo/bar.rs` 对应 `tests/foo_bar_test.rs`，规则需要
   写成可配置的正则，不要硬编码死路径映射）
4. 以上都找不到 → 判定为"无对应测试"，覆盖率缺口按均值填充,
   `confidence` 标记为 `DataMissing("未找到对应测试文件")`

**【禁止事项 4-A】**：不允许在找不到覆盖率报告时，直接把
`覆盖率缺口` 硬编码为 0 或 100 中的任意一个固定值。必须走"用仓库
其他文件均值填充 + 标记 DataMissing"这条路径（呼应原 SPEC 6.4 节
的缺失值处理原则，这条原则现在适用于所有维度，不只是依赖风险）。

**【禁止事项 4-B】**：`断言密度缺口` 不允许只检测 `assert!`、
`assert_eq!` 这类字面关键词而不区分上下文——比如一个测试函数体内
只有函数调用、没有任何断言（这是"测试有壳无肉"的典型特征，必须
能被识别为低质量），如果只做关键词计数而不管上下文，会把这种空
壳测试误判成"有断言"。

**验收方式**：
- 构造一个文件，配一份完整覆盖率报告（覆盖率90%），断言
  `test_risk_value` 明显低于同等复杂度但无测试的文件
- 构造一个"有测试文件但测试函数体内没有任何断言"的场景，断言
  断言密度缺口接近100（说明识别出了"空壳测试"）
- 构造一个完全没有测试信号的文件，断言 `confidence` 是
  `DataMissing`，且该文件的 `risk_score` 使用的是仓库均值填充，
  而不是 0

---

## 5. ChangeRiskAnalyzer（变更风险维度，替代原 history 维度）

### 5.1 核心公式（这是修复"只涨不跌"问题的关键，必须逐字实现）

```
change_risk_value = 近期事故密度 × 0.6 + 近期改动频率 × 0.2 + 作者分散度 × 0.2

近期事故密度 = Σ(每次incident commit的衰减权重) / Σ(同期全部commit的衰减权重)
             衰减权重 = 0.5 ^ (距今月数 / 6)     # 半衰期6个月
近期改动频率 = min(100, 最近90天commit数 / 90 × 100)
作者分散度   = min(100, distinct_authors × 10)   # 权重已降低，不是主导因子
```

**【禁止事项 5-A】**：不允许用"总 commit 数""总 incident 数"这类
终身累计值参与最终公式的任何一环，哪怕只是作为子项之一混进去。
这是本次重构要修复的核心缺陷——只要终身累计值出现在公式里的任何
位置，哪怕权重很小，长期看仍然会让分数单调上升。**必须做代码审查
自查**：搜索整个 `change_risk.rs` 文件，任何地方出现原始的
`commit_count`、`incident_commit_count`（不经过衰减加权处理的）
参与计算，都是违反本节要求的实现，必须改。

**【禁止事项 5-B】**：衰减权重的时间基准不允许用"文件首次出现的
时间"，必须用"运行分析时的当前时间"作为基准点。否则同一次分析里
不同文件的衰减曲线基准不一致，无法横向比较。

**验收方式（这是本次重构最核心的验收项，必须做，不能省略）**：
```
构造一个虚拟文件的两份历史快照：
  快照A（模拟"3年前"）：commit密集、其中2次是incident commit
  快照B（模拟"现在"）：在快照A基础上，追加"最近12个月内只有3次
        commit、0次incident"的新历史

断言：用快照B的完整历史算出的 change_risk_value，必须显著低于
     只用快照A历史算出的 change_risk_value

如果这个测试跑不过，说明衰减机制没有真正生效，必须停下来排查，
不允许跳过这个测试直接进入下一阶段。
```

---

## 6. DependencyAnalyzer（依赖风险维度）

沿用原 SPEC 第 6 节逻辑，**额外新增以下要求**（对应之前报告里发现的
CVE 数据问题）：

**【禁止事项 6-A】**：任何一条 CVE/漏洞记录，如果不是直接来自
OSV API 的实际网络响应（有真实的 HTTP 响应体作为数据来源），
一律不允许出现在最终结果里。开发/测试阶段如果需要 mock 数据，
必须让 mock 数据的来源在代码里有清晰注释标注"此为测试桩数据"，
并且这类 mock 逻辑不能出现在 `--offline` 之外的正常运行路径上。

**【禁止事项 6-B】**：必须做跨数据源去重——如果 GHSA 和 RUSTSEC
两个来源的记录指向同一个底层漏洞（通常 OSV 响应里会有 `aliases`
字段互相关联），只能计为 1 条，不能重复计入 `cve_count`。

**验收方式**：
- 用真实存在漏洞的一个知名 crate/package（比如历史上确实爆过
  RUSTSEC 记录的包）跑一遍，人工去 rustsec.org 或 osv.dev 核实
  返回的每一条记录都真实存在
- 构造一个 mock 场景：同一漏洞在 OSV 响应里同时以 GHSA 和 RUSTSEC
  两个 ID 出现，断言最终 `cve_count` 只计 1 次

---

## 7. scoring.rs（归一合成层）

```
base_risk = w1×complexity_risk + w2×duplication_risk + w3×test_risk
          + w4×change_risk + w5×dependency_risk
          + 高风险维度惩罚（逻辑同原SPEC，仅当某维度显著偏离均值时追加）

默认权重：complexity=0.30 duplication=0.15 test=0.25
         change_risk=0.20 dependency=0.10
```

**【禁止事项 7-A】**：`scoring.rs` 里不允许出现任何一行直接读取
`FileRawMetrics` 原始字段并做计算的代码。它只能消费五个
`DimensionResult`，做加权求和。如果发现 `scoring.rs` 里出现了
类似 `raw.cyclomatic_complexity * 0.4` 这样的代码，说明架构解耦
没有做干净，某个维度的计算逻辑泄漏到了归一层，必须挪回对应的
Analyzer 文件里。

**【禁止事项 7-B】**：五个维度中任意一个如果 `confidence` 是
`NotApplicable`（比如某语言还没实现重复检测），归一计算时不允许
直接把该维度当 0 分参与加权（0 分会拉低总分，造成"这门语言的代码
质量看起来比实际更好"的假象）。正确处理方式：**该维度权重按比例
分摊到其余维度上重新归一**，并在报告里显著标注"本次分析中，
duplication 维度因语言暂不支持而被排除，最终分数基于其余四个维度
重新加权"。

**验收方式**：
- 单元测试直接检查 `scoring.rs` 源码不出现 `FileRawMetrics` 类型
  作为函数参数（用 `cargo doc` 或简单的静态检查脚本验证依赖关系）
- 构造一个场景：五个维度里有一个是 `NotApplicable`，断言归一后的
  权重总和依然是1.0（重新分摊生效），且报告里出现对应的排除说明文案

---

## 8. 报告层调整

**【禁止事项 8-A】**：报告默认视图不允许只展示一个合成的"热力值"
大数字。必须在同一屏幕内并排展示五个维度各自的独立分数（横向进度条
或雷达图任选其一），"综合参考分"作为辅助排序用途放在旁边，不能
喧宾夺主成为唯一可见的数字。

**验收方式**：人工打开生成的 HTML，确认打开报告后第一屏内，任意
一个 top 风险文件都能同时看到五个维度的独立数值，不需要额外点击
展开才能看到。

---

## 9. 架构解耦的硬性验收指标（新增语言不改动打分逻辑）

这是验证"Role 拆分是否真正做到位"的最终测试，必须实际执行，不能
只是理论上说"架构支持"：

```
步骤：
1. 新建一个简单的 mock 语言适配器（比如一个只有if/for两种结构的
   极简"测试语言"，不需要是真实存在的编程语言，用来验证扩展性）
2. 只新增一个文件：lang/mock_lang.rs，实现 LanguageAdapter trait
3. 在语言注册表里加一行把它注册进去

断言：
- analyzers/ 目录下所有文件，git diff 必须为空（没有任何改动）
- scoring.rs，git diff 必须为空
- 用这门mock语言的示例代码跑一次分析，能正确输出复杂度/重复度分数

如果新增语言过程中，任何一个 DimensionAnalyzer 文件被迫修改了，
说明解耦没有做彻底，必须回头检查是不是有语言特定逻辑泄漏到了
打分层里（常见泄漏点：在 ComplexityAnalyzer 里直接 match 语言类型
写 if-else 分支，而不是统一调用 LanguageAdapter 的抽象方法）。
```

---

## 10. 提交前自查清单（开发 AI 每完成一个 Analyzer 都要过一遍）

- [ ] 该 Analyzer 是否存在任何直接使用终身累计值（而非时间衰减/
      窗口统计）参与计算的代码？（对照第5节禁止事项5-A）
- [ ] `Confidence` 的默认路径是不是"先假设数据不全，验证后才标记
      Full"，而不是反过来？
- [ ] 缺失数据时是否用了仓库均值填充，而不是硬编码0或100？
- [ ] 是否有单元测试覆盖"数据缺失""数据异常"这两种边界情况，
      不是只测试"正常数据"这一种路径？
- [ ] 该 Analyzer 文件里有没有直接依赖具体语言类型判断的代码
      （比如 `if language == Rust`），如果有，是否应该改为调用
      `LanguageAdapter` 的抽象方法？
- [ ] 是否跑过 `cargo clippy --all-targets -- -D warnings` 且无警告？

**每次提交前，把这份清单和对应代码一起贴出来，逐条说明是否满足，
不允许笼统回复"已完成"。**
